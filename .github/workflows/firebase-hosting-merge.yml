# name: Deploy to Firebase Hosting on merge
# on:
#   push:
#     branches:
#       - master
# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Install required tools
#         run: sudo apt-get update && sudo apt-get install -y tar
#         if: runner.os == 'Linux'
      
#       - uses: actions/checkout@v2
      
#       - name: Run tests
#         run: npm test
        
#       - name: Install dependencies and build
#         run: npm ci && npm run build

#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: ${{ secrets.GITHUB_TOKEN }}
#           firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NOSSALEI_BD3B6 }}
#           channelId: live
#           projectId: nossalei-bd3b6

name: Deploy to Firebase Hosting on merge

on:
  push:
    branches:
      - master

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14' # Ou a versão do Node.js que seu projeto requer

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build the project
        run: npm run build

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_NOSSALEI_BD3B6 }}
          channelId: live
          projectId: nossalei-bd3b6
